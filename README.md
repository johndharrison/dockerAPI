R Client for Docker Remote API
==========================

### Install 

dockerAPI is not currently on CRAN. If necessary (install.packages("devtools")) and run:

```
devtools::install_github("johndharrison/dockerAPI")
```

